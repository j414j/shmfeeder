[ ] Add T: Copy trait bound to allow PoD structs only
[ ] Add consumers
[ ] Functionalise heartbeat mechanism
[ ] Add feature flags to remove consumer heartbeats or heartbeats entirely during compile time itself
[ ] Make all shared structs repr(C)
[ ] Make MAX_CONSUMERS configurable on startup rather than a compile time constant
