[X] Add T: Copy trait bound to allow PoD structs only
[X] Add consumers
[X] Functionalise heartbeat mechanism
[X] Add feature flags to remove consumer heartbeats or heartbeats entirely during compile time itself
[X] Make all shared structs repr(C)
[ ] Make MAX_CONSUMERS configurable on startup rather than a compile time constant
[ ] Add tests:
  - [ ] Producer rejects queue if another producer already present
  - [ ] Consumer rejects queue if no active producer
  - [ ] Consumer heartbeat slot reservation and freeing
  - [ ] Magic and version verification
  - [ ] Producer able to re attach to a queue with dead producer
